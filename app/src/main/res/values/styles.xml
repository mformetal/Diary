<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="android:textColorHint">@color/half_opacity_gray</item>
        <item name="icon">@color/icons</item>
    </style>

    <style name="AppTheme.Widget" parent="android:Widget" />

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Dialog" parent="@style/Theme.AppCompat.Light.Dialog" />

    <style name="Forum.Dialog.Title" parent="AppTheme.Widget" >
        <item name="android:textSize">25sp</item>
        <item name="android:layout_gravity">start|center_vertical</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:layout_margin">20dp</item>
    </style>

    <style name="AppTheme.EntryToolbar" >
        <item name="android:background">@android:color/transparent</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="colorControlNormal">@color/primary</item>
        <item name="android:textColorPrimary">@color/primary</item>
    </style>

    <style name="Forum.Dialog.Button" parent="Forum.Widget.Button.Borderless" >
        <item name="android:text">@string/dialog_default_button</item>
        <item name="colorButtonNormal">@color/icons</item>
        <item name="colorAccent">@color/primary_dark</item>
    </style>

    <style name="Forum.Widget.Button.Borderless" parent="Widget.AppCompat.Button.Borderless" >
        <item name="android:textColor">@color/primary_text</item>
    </style>

</resources>
